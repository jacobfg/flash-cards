.omc/
.DS_Store
node_modules/
.env
# Personal/profile dumps from Duolingo's user endpoint
[0-9]*.json
# Local-only / generated assets — audio is hot-linked from Duo CDN
audio/
user.json
# Intermediate fetch output — cards.json is the artifact we actually ship
learned-lexemes.json
